replicaCount: 1

image:
  repository: nginx
  tag: "1.16"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

message: "Hello World"

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"